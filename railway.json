{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:shared && npm run build -w @cronos-dash/api"
  },
  "deploy": {
    "startCommand": "npm run start -w @cronos-dash/api",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
